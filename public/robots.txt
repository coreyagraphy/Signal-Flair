# robots.txt — signalflair.ai
# Posture: EXPLICIT OPEN. Signal Flair is an AI Proof Infrastructure company.
# Unlike publishers protecting IP, we WANT to be ingested for both
# training (long-term model knowledge) and live retrieval (citations).
# Every directive below is intentional. This file is itself a proof asset.
# Last reviewed: 2026-06 — re-audit quarterly (UA strings change).

# ─────────────────────────────────────────────
#  OPENAI  (ChatGPT search + browsing + training)
# ─────────────────────────────────────────────
User-agent: OAI-SearchBot      # ChatGPT search index — citation traffic
Allow: /
User-agent: ChatGPT-User       # user-triggered browse from ChatGPT
Allow: /
User-agent: ChatGPT-User/2.0
Allow: /
User-agent: GPTBot             # model training — we OPT IN on purpose
Allow: /

# ─────────────────────────────────────────────
#  ANTHROPIC  (Claude) — split into training/search in 2026
# ─────────────────────────────────────────────
User-agent: ClaudeBot          # training + citation fetch
Allow: /
User-agent: Claude-SearchBot   # search-time retrieval
Allow: /
User-agent: Claude-User        # user-triggered browse
Allow: /
User-agent: anthropic-ai       # legacy UA, still seen
Allow: /
User-agent: claude-web         # legacy UA, still seen
Allow: /

# ─────────────────────────────────────────────
#  PERPLEXITY
# ─────────────────────────────────────────────
User-agent: PerplexityBot      # answer-engine index builder
Allow: /
User-agent: Perplexity-User    # human-triggered visit
Allow: /

# ─────────────────────────────────────────────
#  GOOGLE  (Search + Gemini + AI Overviews)
# ─────────────────────────────────────────────
User-agent: Googlebot          # classic index — feeds AI Overviews
Allow: /
User-agent: Google-Extended    # Gemini training — we OPT IN
Allow: /
User-agent: Google-NotebookLM
Allow: /

# ─────────────────────────────────────────────
#  MICROSOFT / APPLE / META / OTHERS
# ─────────────────────────────────────────────
User-agent: Bingbot            # Copilot via Bing
Allow: /
User-agent: Applebot           # Siri / Apple Intelligence retrieval
Allow: /
User-agent: Applebot-Extended  # Apple AI training — we OPT IN
Allow: /
User-agent: meta-externalagent # Meta AI
Allow: /
User-agent: Amazonbot          # Alexa / Amazon AI
Allow: /
User-agent: DuckAssistBot      # DuckDuckGo AI
Allow: /
User-agent: MistralAI-User
Allow: /
User-agent: cohere-ai
Allow: /
User-agent: CCBot              # Common Crawl — feeds many open models
Allow: /

# ─────────────────────────────────────────────
#  EVERYONE ELSE — open by default
# ─────────────────────────────────────────────
User-agent: *
Allow: /
# If you ever add private routes, gate them here, e.g.:
# Disallow: /admin/
# Disallow: /api/

# Machine-readable guides for agents:
Sitemap: https://signalflair.ai/sitemap.xml
# LLM context file (not a crawl directive, but agents that look for it find it):
# https://signalflair.ai/llms.txt
