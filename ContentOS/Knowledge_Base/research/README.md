# Research documents

Drop markdown/text research here. The local_docs provider searches these
files with full provenance (path, hash, capture date). A leading line
`source_class: primary` marks a document as a primary source.
